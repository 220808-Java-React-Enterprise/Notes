package com.revature.inheritance;

/*
 * For inheritance
 * Purpose of Interface:
 * By default, all fields/attributes are public static final
 * By default, all methods are public abstract = contract methods (must implement and override)
 * Can have multiple interface inheritance
 * Interface are really useful for code reuse-ability
 */
public interface Animal {
    void shake();
}
